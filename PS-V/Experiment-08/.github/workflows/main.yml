name: CI/CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v2

      - name: Build Docker Image
        run: docker build -t experiment-08-app .

      - name: Run Tests
        run: echo "Running tests"

      - name: Deploy
        run: echo "Deploying Experiment 08"